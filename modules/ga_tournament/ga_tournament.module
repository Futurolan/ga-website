<?php

/**
 * @return array
 */
function ga_tournament_theme($existing, $type, $theme, $path)
{
    return array(
        'ga_tournament_front_block' => array(
            'variables' => array(
                'description' => null
            ),
            'template' => 'ga-tournament-front-block'
        )
    );
}