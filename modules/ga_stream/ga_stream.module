<?php

/**
 * @return array
 */
function ga_stream_theme($existing, $type, $theme, $path)
{
    $theme = [];
    $theme['ga_stream_block'] = array(
        'variables' => array(
            'stream' => null,
            'label' => null
        ),
        'template' => 'ga-stream-block'
    );
    return $theme;
}