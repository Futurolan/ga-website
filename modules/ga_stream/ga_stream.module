<?php

/**
 * @return array
 */
function ga_stream_theme($existing, $type, $theme, $path)
{
    return array(
        'ga_stream_block' => array(
            'variables' => array(
                'description' => null
            ),
            'template' => 'ga-stream-block'
        )
    );
}

