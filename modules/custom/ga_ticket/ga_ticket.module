<?php

function ga_ticket_theme($existing, $type, $theme, $path)
{
    return array(
        'ga_ticket' => array(
            'variables' => array(
                'config' => null,
            ),
            'template' => 'ga-ticket'
        )
    );
}