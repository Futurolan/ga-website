<?php

function ga_front_theme($existing, $type, $theme, $path)
{
    return array(
        'ga_front_live' => array(
            'variables' => array(
                'streams' => null
            ),
            'template' => 'ga-front-live'
        )
    );
}