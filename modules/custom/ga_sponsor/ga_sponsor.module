<?php

function ga_sponsor_theme($existing, $type, $theme, $path)
{
    return array(
        'ga_sponsor' => array(
            'variables' => array(
                'config' => null,
            ),
            'template' => 'ga-sponsor'
        )
    );
}