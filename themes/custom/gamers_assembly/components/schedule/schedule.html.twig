<script>
    var activityDay = [
        {% for activityDay in activities %}
        [
            {% for activity in activityDay %}
            ['{{ activity.field_room.entity.getName() }}', '{{ activity.getTitle() }}', new Date('{{ activity.field_date.getValue()[0]['value'] }}'), new Date('{{ activity.field_date.getValue()[0]['end_value'] }}')],
            {% endfor %}
        ],
        {% endfor %}
    ];
</script>

{{ attach_library('gamers_assembly/gamers_assembly.schedule') }}


<div id="timeline"></div>
